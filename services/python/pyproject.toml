[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ezstatus-python"
version = "0.1.0"
description = "Minimal HTTP service for ezstatus"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["."]
